"""Бизнес-слой сервиса gamification.

Единственный слой, которому не позволено знать про FastAPI, SQLAlchemy,
Redis, httpx, ``gamification_auth`` или любой другой пакет сервиса
(``app.api``, ``app.repositories``, ``app.clients``, ``app.auth``) —
только stdlib. Правило проверяется тестом
(``tests/test_business_imports.py``), а не дисциплиной.

Раскладка:

* ``domain/`` — сущности с поведением, перечисления, доменные ошибки,
  словарь ошибок доступа, ролевые политики;
* ``ports.py`` — протоколы, которые реализует инфраструктура:
  репозитории, Unit of Work, выпуск токена, часы;
* ``use_cases/`` — сценарии, зависящие только от портов.
"""
