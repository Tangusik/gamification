"""Тесты против реальной PostgreSQL.

Пакет отделён каталогом, а не только маркером: у него собственный
``conftest.py``, переопределяющий режим хранилища и порядок очистки
состояния. Всё, что здесь лежит, требует поднятого docker-стека и
переменной ``USERS_TEST_DATABASE_URL``; без неё тесты пропускаются, а не
падают.
"""
